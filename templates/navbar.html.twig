{% extends "base.html.twig" %}
{% block navbar %}
    <nav class="nav-menu-area" id="navbar">
        <div class="nav-position-left">
            <a href="{{ path('app_hello') }}" >
                <img src="{{ asset('images/d-and-d.svg') }}" alt="" class="logo">
            </a>
        </div>
        <div class="nav-position-right">
            <div class="nav-button-pannel">
                <a class="nav-link {{ app.request.attributes.get('_route') == 'app_hello' ? 'active' }}" href="{{ path('app_hello') }}">Accueil</a>
                <a class="nav-link {{ app.request.attributes.get('_route') == 'app_player_character_index' ? 'active' }}" href="{{ path('app_player_character_index') }}">Personnages</a>
                <a class="nav-link {{ app.request.attributes.get('_route') == 'app_monster_index' ? 'active' }}" href="{{ path('app_monster_index') }}">Bestiaire</a>
                <a class="nav-link {{ app.request.attributes.get('_route') == 'app_encounter_new' ? 'active' }}" href="{{ path('app_encounter_new') }}" data-turbo="false">Combat</a>
            </div>
        </div>
    </nav>
{% endblock %}