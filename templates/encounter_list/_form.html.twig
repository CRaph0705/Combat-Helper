{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'multiple': 'multiple'}}) }}
    {# {{ form_widget(form.playerCharacters, {'attr': {'multiple': 'multiple'}}) }} #}

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
