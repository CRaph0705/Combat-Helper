<form method="post" action="{{ path('app_player_character_delete', {'id': player_character.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player_character.id) }}">
    <button class="btn">Delete</button>
</form>
