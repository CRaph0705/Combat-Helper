{% extends 'base.html.twig' %}

{% block title %}PlayerCharacter{% endblock %}

{% block body %}
    <h1>PlayerCharacter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ player_character.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ player_character.name }}</td>
            </tr>
            <tr>
                <th>Hp</th>
                <td>{{ player_character.hp }}</td>
            </tr>
            <tr>
                <th>Ac</th>
                <td>{{ player_character.ac }}</td>
            </tr>
            <tr>
                <th>Initiative</th>
                <td>{{ player_character.initiative }}</td>
            </tr>
            <tr>
                <th>HpMax</th>
                <td>{{ player_character.hpMax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_player_character_index') }}">back to list</a>

    <a href="{{ path('app_player_character_edit', {'id': player_character.id}) }}">edit</a>

    {{ include('player_character/_delete_form.html.twig') }}
{% endblock %}
