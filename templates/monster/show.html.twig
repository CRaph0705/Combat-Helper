{% extends 'navbar.html.twig' %}

{% block title %}Monster{% endblock %}

{% block body %}
    <h1>Monster</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ monster.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ monster.name }}</td>
            </tr>
            <tr>
                <th>HP</th>
                <td>{{ monster.HP }}</td>
            </tr>
            <tr>
                <th>AC</th>
                <td>{{ monster.AC }}</td>
            </tr>
            <tr>
                <th>Initiative</th>
                <td>{{ monster.initiative }}</td>
            </tr>
            <tr>
                <th>HpMax</th>
                <td>{{ monster.hpMax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_monster_index') }}">back to list</a>

    <a href="{{ path('app_monster_edit', {'id': monster.id}) }}">edit</a>

    {{ include('monster/_delete_form.html.twig') }}
{% endblock %}
