<form method="post" action="{{ path('app_monster_delete', {'id': monster.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ monster.id) }}">
    <button class="btn">Delete</button>
</form>
